# BAI 4
money = int(input("Nhap so tien : "))
beer = money // 28
total = beer
shell = beer
while shell >= 3 :
    new_beer = shell // 3
    total += new_beer
    shell = shell % 3 + new_beer
print("Tong so bia co the uong:",total)

